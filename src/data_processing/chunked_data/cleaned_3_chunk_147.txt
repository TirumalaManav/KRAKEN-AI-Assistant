color: var(--white);
border-radius: 12px;
box-shadow: var(--shadow);
animation: fadeIn 1s ease-in;
}
@keyframes fadeIn {
from {
opacity: 0;
}
to {
opacity: 1;
}
}
.header-title h1 {
font-size: 1.8rem;
margin-bottom: 0.5rem;
}
.highlight {
color: var(--cyan);
font-weight: 500;
}
/* Buttons */
.btn {